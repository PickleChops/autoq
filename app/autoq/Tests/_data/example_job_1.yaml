name: This is sample job 1
connection: default
schedule: every day at 4pm
query: |
    Select * from massive_table
    where some_condition is true
    group by 1
outputs:
  - type: s3
    bucket: an_s3_bucket
    format: csv
  - type: email
    address: boydi@boydi.com
    style: html
  - type: email
    address: sid@boydi.com
    style: html

name: Sample job updated for sure
connection: default
schedule: Every Tuesday at 9am
query: |
    Select * from massive_table
    where some_condition is true
    group by 1
outputs:
  - type: s3
    bucket: an_s3_bucket
    format: csv
  - type: email
    address: boydi@boydi.com
    format: html
  - type: email
    address: sid@boydi.com
    format: html

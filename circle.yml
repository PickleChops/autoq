machine:
  environment:
   COMPOSE_CONFIG_FILES: -f base.yml -f test.yml
  services:
    - docker

checkout:
  post:
   - cp ./app/autoq/travis.env ./app/autoq/.env

dependencies:
  override:
    - make compose
    - make up
    - docker ps

test:
  override:
    - make tests
